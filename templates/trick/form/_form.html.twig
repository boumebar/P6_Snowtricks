{{ form_start(formView)}}
{{ form_errors(formView)}}

<div class="row my-5">
	<div class="col-lg-8 mx-auto">
		{{ form_widget(formView)}}
		<div class="text-center">
			<button type="submit" class="btn btn-outline-primary mt-4">
				<i class="fas fa-save"></i>
				{{ submitButtonText ?? "Ajouter" }}</button>
		</div>
		{{ form_end(formView)}}
	</div>
</div>
